pipeline {      
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "running from master"'
            }
        }
        stage('Build feature') {
            steps {
                sh 'echo "running from feature1"'
            }
        }
  }
}
